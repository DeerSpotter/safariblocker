name: Build SafariBlocker deb

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate package layout
        run: |
          test -f package/DEBIAN/control
          test -d package/var/jb
          test -d assets/hex
          find package -type f | sort

      - name: Restore binary payload files
        run: scripts/rebuild-binaries.sh package

      - name: Build deb package
        run: |
          set -euo pipefail
          mkdir -p dist
          chmod 0755 package/DEBIAN
          chmod 0644 package/DEBIAN/control
          dpkg-deb --root-owner-group --build package dist/com.p2kdev.safariblocker_v1.3.1_iphoneos-arm64.deb
          sha256sum dist/com.p2kdev.safariblocker_v1.3.1_iphoneos-arm64.deb | tee dist/SHA256SUMS

      - name: Upload deb artifact
        uses: actions/upload-artifact@v4
        with:
          name: safariblocker-deb
          path: |
            dist/*.deb
            dist/SHA256SUMS

      - name: Attach deb to GitHub release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*.deb
            dist/SHA256SUMS
